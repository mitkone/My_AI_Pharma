# Pharma Data Viz

Приложение за визуализация на фармацевтични продажби. Всички Excel файлове се зареждат от папката – колегите избират медикамент от обща списък, без да качват файлове.

## За теб (админ)

1. **Слагаш Excel файловете** в папката `My_AI_Pharma` (Lipocante, Antihistamines, Valsavil и т.н.)
2. **Стартираш** приложението: `run_app.bat` или `streamlit run app.py`
3. При **нови данни**: добавяш нови Excel в папката → в sidebar натискаш „Обнови данните“ (или рестартираш приложението)
4. Колегите **не качват файлове** – виждат готовите данни и избират медикамент от една обща списък (всички категории)

## За колегите (крайни потребители)

1. **Стартиране:** Двойно кликване на `run_app.bat` (или отварят връзката, ако е на уеб)
2. **Използване:** Избират Регион → Медикамент → Brick → гледат графиките и AI анализа
3. Няма качване на файлове – данните са предварително заредени

### Как да дадеш приложението на други

**Вариант A – локално (на тяхен компютър)**  
Копирай цялата папка `My_AI_Pharma` (включително Excel файловете). Те пускат `run_app.bat`.

**Вариант B – уеб (Streamlit Community Cloud)**  
1. Качи проекта в GitHub (включително Excel файловете или ги добави след deploy)  
2. Регистрирай се на [share.streamlit.io](https://share.streamlit.io)  
3. Deploy от репото – колегите отварят връзка и работят през браузър

**Streamlit Community Cloud е безплатен** – 1 частен (private) ап бесплатно, неограничени публични. За повече private ап-ове има платени опции през Snowflake.

## Инсталация (ако нямаш Python)

1. Инсталирай [Python](https://www.python.org/downloads/) (отбележи „Add to PATH“)
2. Отвори Command Prompt в папката и изпълни:
   ```
   pip install -r requirements.txt
   ```
3. Стартирай `run_app.bat`

## Молекули и препарати

- Всеки Excel файл съдържа различни молекули: **Antihistamines** (Desloratadine, Bilastine…), **Statins** (Lipocante/Pitavastatin, Atorvastatin…), **Kaldera SPM** (Ticagrelor, Clopidogrel…), **Valsavil** (ARB-ове) и др.
- Ако препарат е непознат (Other): добави го от sidebar „➕ Добави молекула“ в приложението
- **update_molecules.py** – сканира всички Excel файлове и показва познати/непознати молекули:
  ```
  python update_molecules.py
  ```

## AI Анализ

В таб **🤖 AI Анализ** можеш да зададеш въпрос за данните, напр.:

> *„Защо Lipocante в Sofia East не расте толкова много колкото в Pleven?“*

AI ще анализира наличните данни и ще предложи възможни причини и решения.

**Настройка на API ключа (OpenAI):**

1. Създай файл `.env` в папката `My_AI_Pharma` (същата, където е `app.py`)
2. На един ред напиши (без кавички, без интервали около `=`):
   ```
   OPENAI_API_KEY=sk-proj-твой-ключ-тук
   ```
3. Инсталирай `python-dotenv`: `pip install python-dotenv`
4. Рестартирай приложението

Ако все още не работи: отвори Command Prompt в папката и задай променливата ръчно преди стартиране:
```
set OPENAI_API_KEY=sk-proj-твой-ключ
python -m streamlit run app.py
```

**Streamlit Cloud:** Ако deploy-ваш онлайн, добави ключа в Dashboard → Secrets: `OPENAI_API_KEY=sk-...`

## Формат на Excel файловете

- Колона A: Region → District (Brick) → Category → Drug
- Колони B–M (или повече): Периоди – Q1 2023, Q2 2023… или Jan 2023, Feb 2023…
